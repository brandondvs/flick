services:
  database:
    image: postgres:18.3-alpine3.23
    restart: always
    environment:
      POSTGRES_PASSWORD: flick
    ports:
      - 5432:5432
    volumes:
      - .flick-data:/var/lib/postgresql

  adminer:
      image: adminer
      restart: always
      ports:
        - 8080:8080
